────────────────────────────────────────────────────────────────
WaveEye Diagnostic Summary
────────────────────────────────────────────────────────────────
Primary Failure    :
  AXI4L_RDATA_STABILITY  (AXI4-Lite Spec §A3.3.1)

Confirmed Root Cause :
  RDATA driver has no RREADY gate — payload advances freely during backpressure

Severity           :
  Handshake violation — AXI compliance failure

Transactions Affected :
  Protocol-visible failures     : 1
  Structurally corrupted writes : 0
────────────────────────────────────────────────────────────────
────────────────────────────────────────────────────────────────
Causal Trace (Condensed)
────────────────────────────────────────────────────────────────
  Read transaction accepted
    RVALID handshake observed.

  Signal stability violation — RDATA mutated during RVALID window
    RDATA changed value while RVALID=1 and RREADY=0 — AXI4-Lite Spec §A3.3.1 requires RDATA to remain stable from the first RVALID cycle until RVALID∧RREADY handshake (mutation at cycle 316).

  Control logic evaluated
    FSM state analysis completed — scheduling conflict or missing enable condition identified in R-channel logic.

  s_axi_rdata driver predicate identified  [PROOF]
    s_axi_rdata @ t_eval=305
    Active condition: if (true)

  MODE-C — Stability mutation  [PROOF]
      Stability window  : RVALID asserted @ t=296  until RREADY handshake
  Signal mutated    : RDATA  0x22041195 → 0  @ cycle 315

  Active driver @ t_mut=315:
    D0: if (true) → (hold_read_resp)  ? s_axi_rdata_hold : read_data  ★ ACTIVE
    TRUE terms: true=TRUE

  ↳ ROOT CAUSE: RDATA driver has no RREADY gate.
    RTL Fix: gate the update on (!RREADY) or register
    RDATA at transaction-accept time.

  Missing required AXI dependencies  [PROOF]
    ✗  Absent from predicate: RVALID, RREADY
    → s_axi_rdata is driven by local FSM only — required handshake signals never gated.

  Conclusion:
  Protocol failure is structurally guaranteed — predicate contains no path to verify RVALID, RREADY.
────────────────────────────────────────────────────────────────