# ── Cython build artefacts ────────────────────────────────────────────────
# Compiled extensions (Windows .pyd, Linux/macOS .so)
*.pyd
*.so

# Cython-generated C files (safe to delete; regenerated by build)
**/*.c

# Cython HTML annotation files
**/*.html

# setuptools/distutils build tree
build/
# dist/ wheels are committed for distribution (see dist/)
# dist/

# Egg / wheel metadata
*.egg-info/
*.egg

# ── Python ────────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo

# ── PyInstaller standalone ────────────────────────────────────────────────
standalone/

# ── Build logs ────────────────────────────────────────────────────────────
build_log.txt

# ── Closed-source core engine (compiled .pyd/.so distributed separately) ─
IR_backtracking/rca_8.py
IR_backtracking/rca_resolver.py
IR_backtracking/rca_core/
IR_backtracking/deep_backtrack.py
IR_backtracking/cyclic_dependency_analyzer.py
IR_backtracking/IR_backtracking.py
IR_backtracking/alias_engine.py
IR_backtracking/temporal_predicate_analysis.py
IR_backtracking/fsm_illegal_state.py
IR_backtracking/inter_fsm.py
IR_backtracking/grouping.py
IR_backtracking/extract_fsm_encodings.py
IR_backtracking/reports/causal_pruner.py

# ── Archive / legacy code ─────────────────────────────────────────────────
_archive_main_tool/

# ── Runtime outputs (not source) ──────────────────────────────────────────
*.proof.json
*.appendix.txt
causal_binding.json

# ── WaveEye user output directories ──────────────────────────────────────
outputs/

# ── IDE / OS ──────────────────────────────────────────────────────────────
.vscode/
.idea/
.DS_Store
Thumbs.db
